# BUG 调试日志

## 2026-05-07 首页“解析”按钮无效，页面刷新，前后端无法联动

### 严重级别

- 高

### 现象描述

- 首页输入视频链接后点击“解析”，页面没有进入工作台。
- 页面表现为刷新回当前首页。
- FastAPI 后端日志没有收到 `/api/media/info` 或相关任务请求。
- 用户无法继续测试字幕、总结、思维导图、AI 对话等后续功能。

### 影响范围

- 首页主入口不可用。
- 前后端联调被阻断。
- 工作台功能整体不可验证。

### 复现方式

1. 启动 FastAPI：
   - `uvicorn app.main:app --reload --host 127.0.0.1 --port 8000`
2. 启动 Next.js：
   - `npm run dev`
3. 打开首页。
4. 输入公开视频链接。
5. 点击“解析”。

### 实际结果

- 页面刷新。
- 不跳转到 `/workspace`。
- 后端无解析请求日志。

### 期望结果

- 点击“解析”后，前端调用 `/api/video/info`。
- 解析成功后跳转到 `/workspace?url=...`。
- 工作台继续调用后端字幕、总结、聊天等接口。

### 排查记录

#### 1. 现象判断

- 首页原实现使用原生 `<form onSubmit={handleAnalyze}>`。
- 虽然代码里有 `event.preventDefault()`，但一旦客户端 hydration 未及时接管，浏览器仍可能直接执行原生表单提交。
- 原生提交会触发一次普通 GET 刷新，因此用户看到的是“按钮无效、页面刷新”。

#### 2. 环境侧发现

- 调试过程中一度发现 Next.js 开发服务本身返回过 `502`。
- 在这种不稳定状态下，即使页面看起来打开了，客户端事件绑定也可能不完整，从而进一步放大原生表单刷新的问题。

#### 3. 根因判断

本次 BUG 的主要风险点有两个：

1. 首页解析入口依赖原生表单提交语义，在 hydration 不稳定时容易退化成页面刷新。
2. 前端本地开发时若没有显式配置 `SAVEANY_BACKEND_URL`，工作台相关 BFF 路由默认无法联到 FastAPI，前后端联调门槛过高。

### 已实施修复

#### 修复 1：首页解析入口改为纯按钮触发

修改文件：

- [app/page.tsx](/D:/Codex_Project/SaveAny/app/page.tsx)

修复动作：

- 去掉首页对原生表单提交的依赖。
- 将解析操作改为 `type="button"` 直接触发。
- 为输入框补充回车逻辑，手动调用解析函数。
- 在调用前先做 `trim()` 和空值检查。

修复目标：

- 即使 hydration 稍慢，也不再触发整页原生刷新。

#### 修复 2：本地开发默认联到 FastAPI

修改文件：

- [lib/backend-client.ts](/D:/Codex_Project/SaveAny/lib/backend-client.ts)

修复动作：

- 在非生产环境下，如果未配置 `SAVEANY_BACKEND_URL`，默认使用：
  - `http://127.0.0.1:8000`

修复目标：

- 降低本地联调门槛。
- 让字幕、总结、AI 对话等依赖 FastAPI 的功能默认可联通。

### 当前结论

- 该 BUG 已被记录并已实施代码层修复。
- 当前最关键的入口风险已经从“原生表单刷新”改为“纯客户端按钮触发”。
- 本地开发下的前后端地址也已默认打通。

### 待验证项

1. 重启前端与后端后，首页点击“解析”是否稳定跳转到工作台。
2. `/workspace` 是否能继续触发：
   - 字幕提取
   - 总结任务
   - 思维导图展示
   - AI 对话
3. 前端 dev server 是否仍偶发 `502`。

### 建议

- 后续所有关键入口按钮，尽量避免把主交互完全绑定在原生表单提交上。
- 对本地开发依赖的后端地址提供默认值，减少“页面能开但功能不通”的假故障。
- 若后续再次出现“点击无效但页面刷新”的问题，优先检查：
  - 客户端 hydration 是否成功
  - Next.js dev server 是否健康
  - 页面是否仍存在原生 submit 路径
