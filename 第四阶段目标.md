# SaveAny 第四阶段目标：付费功能核心开发

生成时间：2026-05-06

## 阶段定位

第四阶段的核心不是 UI 精修，而是把未来 Pro 功能的技术闭环先做出来。

接下来你会在另一个窗口用竞品分析的方式拆解视频总结、字幕翻译等功能。该竞品分析结果将作为第四阶段的需求输入，本阶段需要在此基础上形成可执行方案，再人工确认，再分步骤开发。

## 阶段目标

```text
下载功能已经完成
-> 基于现有 FastAPI 任务模型
-> 开发视频总结、字幕提取、字幕翻译等 Pro 功能
-> 功能跑通后再进入体验精修
```

## 优先级

### P0：视频总结

目标：

- 用户提交视频链接。
- 后端创建 `summarize` 任务。
- 优先获取字幕；没有字幕时提取音频。
- 将字幕或音频转写结果交给 LLM。
- 返回结构化总结结果。

建议结果结构：

```text
summary
highlights
chapters
timestamps
keywords
sourceLanguage
```

验收标准：

```text
公开视频链接
-> 创建总结任务
-> 查询任务状态
-> 返回摘要、章节、要点、时间戳
```

### P0：字幕提取

目标：

- 优先读取平台已有字幕。
- 支持 `yt-dlp` 字幕能力。
- 没有字幕时，为语音转写保留入口。

建议任务类型：

```text
subtitle_extract
```

验收标准：

```text
公开视频链接
-> 创建字幕提取任务
-> 返回字幕文本或明确说明无字幕
```

### P1：音频提取

目标：

- 使用 `yt-dlp` 或 `ffmpeg` 提取音频。
- 为语音转文字、视频总结、仅音频下载提供基础能力。

建议任务类型：

```text
audio_extract
```

验收标准：

```text
视频链接或已下载视频
-> 提取音频
-> 得到可用于转写的音频文件
```

### P1：字幕翻译

目标：

- 对提取到的字幕进行翻译。
- 支持至少中文和英文方向。
- 返回原文字幕、译文字幕和分段结构。

建议任务类型：

```text
subtitle_translate
```

验收标准：

```text
已有字幕
-> 选择目标语言
-> 返回翻译后的字幕文本
```

### P2：批量任务

目标：

- 为批量下载、批量总结预留任务模型。
- MVP 阶段先不做大规模并发。

建议任务类型：

```text
batch_download
batch_summarize
```

## 后端接口草案

```text
POST /api/tasks/summarize
POST /api/tasks/subtitle
POST /api/tasks/audio
POST /api/tasks/translate
GET  /api/tasks/{task_id}
```

当前已有 `GET /api/tasks/{task_id}`，第四阶段应复用统一任务状态，不要为每个功能单独发明一套状态模型。

## 数据模型扩展

当前任务模型已有：

```text
taskId
type
status
progress
fileName
fileUrl
error
code
createdAt
updatedAt
expiresAt
ownerId
entitlement
result
```

第四阶段重点扩展 `result`：

```text
result.summary
result.chapters
result.highlights
result.timestamps
result.subtitles
result.translation
result.audioFileUrl
```

## 技术路线

建议保持简单：

- 后端继续使用 FastAPI。
- 任务表继续使用内存结构，先跑通能力。
- 视频/音频处理继续使用 `yt-dlp` + `ffmpeg`。
- LLM、ASR、翻译服务先封装为 service 层，具体供应商由竞品分析和成本评估后确认。
- 前端先做最小工作台，不优先做视觉精修。

## 暂不做

- 真实支付。
- 账号系统。
- 长期保存历史。
- 大规模并发队列。
- 复杂权限系统。
- 完整后台管理系统。

## 第四阶段验收标准

```text
下载功能保持可用
并且至少跑通：
1. 一个视频总结任务
2. 一个字幕提取任务
3. 一个字幕翻译或音频提取任务
4. 前端能查看任务结果
5. 失败时返回结构化错误
```
