# SaveAny 第三阶段执行报告

生成时间：2026-05-06

## 阶段目标

第三阶段目标是把下载能力从 Next.js 单体 MVP 中拆出，形成可扩展的 FastAPI 后端雏形，为后续视频总结、字幕翻译、音频提取等 Pro 功能提供任务基础。

## 已完成内容

### 1. 新增 FastAPI 后端

新增目录：

```text
backend/
  app/
    api/
    core/
    models/
    services/
  storage/
  requirements.txt
  README.md
```

核心接口：

```text
GET  /health
GET  /api/system/check
POST /api/media/info
POST /api/tasks/download
GET  /api/tasks/{task_id}
GET  /api/files/{file_id}
GET  /api/thumbnails/{thumbnail_id}
```

### 2. 后端能力拆分

FastAPI 当前负责：

- 平台识别。
- Resolver 调度。
- 视频信息解析。
- 缩略图缓存。
- 下载任务创建。
- 下载任务状态查询。
- 临时文件返回。
- 系统环境自检。

### 3. 下载策略

- 抖音：走 `DouyinResolver`，后端代理下载公开视频直链。
- YouTube/Bilibili：走 `YtDlpResolver` 和 `yt-dlp`。
- 文件只写入 `backend/storage/downloads/{taskId}`。
- 缩略图写入 `backend/storage/thumbnails/{thumbnailId}`。
- 公网 MVP 默认不接收用户 Cookie。

### 4. Next.js BFF 接入

新增：

```text
lib/backend-client.ts
```

Next.js 接口：

```text
POST /api/video/info
POST /api/video/download
```

配置：

```text
SAVEANY_BACKEND_URL=http://127.0.0.1:8000
```

后会优先调用 FastAPI；FastAPI 不可用时回落 Next.js 内置能力。

## 验证结果

已通过：

```text
python -m compileall backend/app
npm run lint
```

接口验证：

- `/health` 返回 `ok`。
- `/api/system/check` 检测到 `yt-dlp 2026.03.17`。
- `/api/system/check` 检测到 `ffmpeg 2026-04-30`。
- 空 URL 返回 `INVALID_URL`。
- 抖音精选链接解析成功，使用 `DouyinResolver`，识别 27 个格式，推荐 1080p。
- 抖音 720p 下载任务成功，生成约 12MB 文件。
- `/api/files/{taskId}` 能正常返回下载文件。
- YouTube 公开视频解析成功，使用 `YtDlpResolver`。
- YouTube 缩略图缓存接口返回 `image/jpeg`。

受限项：

- 本机已有 Next dev server 占用项目锁，新的 `next dev --port 3100` 被 Next.js 拒绝启动，因此未在第二个端口完成 BFF 端到端验证。
- 需要重启当前前端服务并带上 `SAVEANY_BACKEND_URL` 后进行人工验收。

## 当前结论

下载功能到第三阶段已经完成，可以沉淀为项目基础能力。

后续不应继续围绕下载做大改，而应进入第四阶段：基于现有任务模型开发视频总结、字幕提取、字幕翻译、音频提取等 Pro 功能。前端体验精修顺延到功能闭环之后。

## 第四阶段承接

第四阶段新增目标文件：

```text
第四阶段目标.md
```

下载功能沉淀文件：

```text
SaveAny下载功能沉淀.md
```
