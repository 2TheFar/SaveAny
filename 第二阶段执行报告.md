# SaveAny 第二阶段执行报告

生成时间：2026-05-06

## 1. 本阶段完成内容

已根据《第二阶段目标.md》完成第一轮架构落地：

- 新增平台识别层。
- 新增结构化错误码。
- 新增 Resolver 抽象。
- 新增 `YtDlpResolver`。
- 新增 `DouyinResolver`，已接入抖音精选页无 Cookie 公开解析与后端代理下载。
- 新增缩略图后端接口。
- 缩略图缓存增加 `curl` 兜底，解决当前环境下 Node `fetch` 访问 YouTube 缩略图失败的问题。
- 前端改为展示后端返回的真实质量选项。
- 前端展示平台和 Resolver 信息。
- B 站 1080p 保持由 `YtDlpResolver` 支持，作为当前已验证能力保留。

## 1.1 抖音轮子选型结论

本轮没有直接绑定某一个仓库作为黑盒依赖，而是先做了轻量选型：

| 项目 | 结论 |
| --- | --- |
| `jiji262/douyin-downloader` | 社区认可度高，功能完整，但偏批量下载、配置和 Cookie/浏览器兜底，当前 MVP 接入成本偏高。 |
| `pwh-pwh/douyinVd` | TypeScript、MIT、部署形态接近 SaveAny，适合作为无 Cookie 单链接解析思路参考。 |
| `rathodpratham-dev/douyin_video_downloader` | MIT、代码轻量，解析思路清晰，但社区认可度低，不适合直接作为唯一依赖。 |

当前采用“借鉴成熟思路 + SaveAny 内部轻量 Resolver”的方式：

- 抖音不再交给 `yt-dlp`。
- 优先解析抖音页面 SSR/Flight 数据。
- 读取页面内的 `videoDetail`、封面和 `bitRateList`。
- 下载由 SaveAny 后端代理直链完成。
- 不接收、不保存、不使用用户 Cookie。

## 2. 已实现接口变化

`POST /api/video/info` 返回内容增强：

```text
platform
resolverUsed
requiresCookie
availableQualities
recommendedQuality
thumbnailUrl
```

新增：

```text
GET /api/video/thumbnail/:id
```

用于返回后端缓存缩略图或 SaveAny 占位缩略图，避免前端直连第三方图片。

## 3. 已验证结果

### 构建与类型

- `npm run lint` 通过。
- `npm run build` 通过。

### YouTube 测试

测试链接：

```text
https://www.youtube.com/watch?v=jNQXAC9IVRw
```

结果：

- `platform = youtube`
- `resolverUsed = YtDlpResolver`
- 返回 `availableQualities`
- 返回 `recommendedQuality`
- 返回 SaveAny 本站 `thumbnailUrl`
- 缩略图接口返回真实 `image/jpeg`，不再落到默认占位图
- 下载成功
- 文件接口下载成功，测试文件约 474 KB

### 抖音测试

测试链接：

```text
https://www.douyin.com/jingxuan?modal_id=7630088455829370442
```

结果：

- `platform = douyin`
- `resolverUsed = DouyinResolver`
- `requiresCookie = false`
- 成功返回真实封面 `thumbnailUrl`
- 成功返回真实质量选项：`best / 1080p / 720p`
- 推荐质量：`1080p`
- 720p 下载成功
- 文件接口返回成功，测试文件约 12 MB

说明：本次解决的是 `jingxuan?modal_id=...` 这类公开页面。普通分享短链仍需要继续补充样本测试。

### 前端验证

- 首页可访问。
- 质量按钮支持后端可用性状态。
- 页面文案已从单纯 `yt-dlp` 改为多解析器架构。
- 桌面端截图通过。

## 4. 当前保留限制

本阶段没有实现：

- FastAPI 服务。
- 下载进度。
- 任务队列。
- 数据库。
- 用户 Cookie 上传。
- 账号与支付。

这些仍属于后续阶段。

## 5. 下一步建议

下一步应聚焦 `DouyinResolver` 的样本覆盖和稳定性验证：

1. 收集公开视频样本。
2. 验证短链跳转。
3. 验证普通 `/video/{id}` 链接。
4. 验证图文、失效链接、登录限制、风控页面的错误归类。
5. 继续评估是否需要引入独立 FastAPI 下载服务。
6. 为抖音直链下载增加更清晰的进度状态。

如果抖音样本覆盖稳定，再进入 FastAPI 下载服务雏形阶段。
